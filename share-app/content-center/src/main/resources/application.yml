server:
  port: 8090
spring:
  application:
    name: content-center
  cloud:
   nacos:
    discovery:
      server-addr: localhost:8848
#      名称空间
#      namespace: 4eef7a74-a5bd-4ee2-b770-90b209d5417f
  datasource:
    url: jdbc:mysql://39.97.173.29:3306/content_center
    hikari:
      username: root
      password: abc.123
      driver-class-name: com.mysql.cj.jdbc.Driver

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

user-center:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule

ribbon:
  eager-load:
    enabled: true
    clients: user-center

logging:
  level:
    # feign的日志系统是建立在SpringBoot基础上的，此处一定要是debug，否则不会打印
    com.soft1851.contentcenter.feignclient.UserCenterFeignClient: debug
#feign:
#  client:
#    config:
#      # 全局配置
#      default:
#        loggerLevel: full
#  httpclient:
#    # 让feign使用apache httpclient做请求；而不是默认的urlconnection
#    enabled: true
#    # feign的最大连接数
#    max-connections: 200
#    # feign单个路径的最大连接数
#    max-connections-per-route: 50